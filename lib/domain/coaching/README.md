# Coaching domain

This directory owns deterministic, framework-free coaching calculations.
Numerical functions calculate expected intake, pace status, remaining rate,
bottle equivalents, and checkpoints. Message selection consumes those results
and never owns numerical truth. No AI service is used.
